# Vite exposes only VITE_-prefixed env vars to the browser. Anything else stays server-side.
# Leave empty to use the dev-server proxy (recommended — same-origin cookies "just work").
# Set to an absolute URL only when the API lives on a different origin (e.g., a staging backend).
VITE_API_URL=
